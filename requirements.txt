# Core ML — CPU-only to keep Docker image lean
torch --index-url https://download.pytorch.org/whl/cpu
transformers
sentence-transformers
# pin numpy to avoid torch CPU conflict
numpy>=1.24.0,<2.0.0

# Vector database
chromadb

# UI
# bumped from 1.33.0 (too old, gap param broken)
streamlit>=1.35.0

# HTTP + data
requests
pandas

# Text processing
# pin to v2, demojize() API changed between v1 and v2
emoji>=2.0.0

# Ollama Python SDK
# pin to avoid breaking changes
ollama>=0.1.8

# removed langchain and langchain-community (unused, +500MB image bloat)
